# -*-perl-*-

package.NodeJS-audible-api = {
  interfaces = (1.0);

  scope = third-party;

  third-party-configuration = {
    # The thirdPartyVersion is specified for backwards compatibility.
    # Please do not remove it.
    thirdPartyName      = "audible-api";
    thirdPartyVersion   = "0.1.0";
    licenseType         = "TheUnlicensed";
    licenseFile         = "audible-api/TheUnlicensed";
    repository          = "raw";
    approvedTicketNumber= "";
  };

  build-environment = {
    chroot = basic;
    network-access = blocked;
  };

  build-system = npm-build;
  build-tools = {
      1.0 = {
      NpmBuild = 1.0;
    };
  };

  dependencies = {
    1.0 = {
      NpmBuild = 1.0;
    };
  };

  targets = {
    NodeJS-audible-api = {
      type = thirdparty;
    };
  };

};
